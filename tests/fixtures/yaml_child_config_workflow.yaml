config:
  entrypoint: main

models:
  default:
    provider: openai
    model: parent-model
    api_url: http://localhost:11434

flows:
  main:
    steps:
      - name: run_child
        uses: yaml
        file: external_child_workflow.yaml
        input:
          summary: static
