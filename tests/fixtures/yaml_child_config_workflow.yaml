config:
  model: parent-model
  entrypoint: main

flows:
  main:
    steps:
      - name: run_child
        uses: yaml
        file: external_child_workflow.yaml
        input:
          summary: static
