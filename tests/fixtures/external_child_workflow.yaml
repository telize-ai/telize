config:
  entrypoint: main

models:
  default:
    provider: openai
    model: child-only-model
    api_url: http://localhost:11434

flows:
  main:
    steps:
      - name: format_alert
        uses: shell
        run: echo "model={{ models.default.model }} summary={{ input.summary }}"
