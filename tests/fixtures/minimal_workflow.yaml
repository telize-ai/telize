config:
  model: test/model
  entrypoint: main

flows:
  main:
    steps:
      - name: greet
        uses: shell
        run: echo "hello telize"
      - name: echo_greet
        uses: shell
        run: echo "{{ steps.greet.output }}"
