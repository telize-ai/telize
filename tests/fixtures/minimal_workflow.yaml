config:
  model: test/model
  entrypoint: main

flows:
  main:
    steps:
      - name: greet
        uses: shell
        run: echo "hello telize"
      - name: echo_greet
        uses: shell
        prompt: "Repeat back: {{ steps.greet.output }}"
